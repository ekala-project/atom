[atom]
name = "dev"

[fetch.pkgs]
name = "nixpkgs"
import = true
args = [{}]

[std]
features = ["pkg_lib"]

[backend.nix]
# default when `eka` cli is ready
# fetcher = "native"
fetcher = "npins"

[fetcher.npins]
root = "npins"
